# 国内云服务器部署（无需 GitHub，推荐）

适合 GitHub / Render 连不上的情况。购买后获得固定 IP，手机流量随时可访问。

---

## 你需要准备

| 项目 | 推荐 | 费用 |
|------|------|------|
| 云服务器 | 腾讯云或阿里云「轻量应用服务器」 | 约 50–88 元/月 |
| 系统 | **Ubuntu 22.04** | — |
| 配置 | 1核 2G 内存 | 足够使用 |

---

## 第一步：购买服务器

### 腾讯云（推荐）

1. 打开 https://cloud.tencent.com/product/lighthouse
2. 选轻量应用服务器，地域选离球馆近的（如广州、上海）
3. 镜像选 **Ubuntu 22.04**
4. 购买后记下：**公网 IP**（如 `123.45.67.89`）

### 阿里云

1. 打开 https://www.aliyun.com/product/swas
2. 同样选 Ubuntu 22.04 轻量服务器

### 开放端口（重要）

在云平台控制台 → 防火墙 / 安全组，放行：

- **22**（SSH 登录）
- **80**（网页访问）
- **443**（HTTPS，可选）

---

## 第二步：上传项目到服务器

### 方式 A：Mac 终端上传（推荐）

在 Mac 终端依次运行：

```bash
# 1. 打包项目（已排除 node_modules）
cd /Users/rae/Documents
zip -r badminton.zip badminton-court-manager -x "*/node_modules/*" -x "*/.git/*"

# 2. 上传到服务器（把 IP 换成你的公网 IP）
scp badminton.zip root@你的服务器IP:/root/
```

第一次连接会问 `Are you sure...`，输入 `yes` 回车。  
然后输入服务器 root 密码（购买时邮件/控制台里有）。

### 方式 B：控制台网页上传

1. 先在 Mac 打包（运行上面 zip 命令）
2. 登录云服务器 **网页终端**（控制台里点「登录」）
3. 用控制台的文件上传功能，把 `badminton.zip` 传到 `/root/`

---

## 第三步：安装并启动

SSH 或网页终端登录服务器后运行：

```bash
cd /root
apt update -qq
apt install -y unzip
unzip -o badminton.zip
cd badminton-court-manager
chmod +x deploy/install.sh
./deploy/install.sh
```

> 如果提示权限不够，改用：`sudo ./deploy/install.sh`

安装完成后终端会显示：

```
✅ 部署完成！
访问地址: http://你的服务器IP
```

---

## 第四步：设置登录密码

安装脚本会自动生成 `.env` 文件，查看管理员密码：

```bash
cat /root/badminton-court-manager/.env | grep ADMIN
```

修改密码：

```bash
nano /root/badminton-court-manager/.env
```

把 `ADMIN_PASSWORD=` 改成你的密码，保存后重启：

```bash
cd /root/badminton-court-manager
docker compose -f docker-compose.run.yml restart
```

---

## 完成！

- **电脑访问**：`http://你的服务器IP`
- **手机访问**：同一地址，收藏到浏览器书签
- **账号**：`admin`（或 .env 里设置的）
- **密码**：你在 .env 里设置的

服务器 24 小时在线，电脑可以关机。

---

## 绑定域名（可选）

1. 在万网/腾讯云购买域名
2. DNS 添加 A 记录 → 指向服务器 IP
3. 运行 SSL 脚本：

```bash
cd /root/badminton-court-manager
# 先编辑 .env 设置 DOMAIN=你的域名.com
nano .env
sudo ./deploy/ssl-setup.sh
```

之后用 `https://你的域名.com` 访问。

---

## 常见问题

**scp 连接失败？**  
检查服务器 IP、密码，以及防火墙是否开放 22 端口。

**网页打不开？**  
检查防火墙是否开放 **80** 端口；运行 `docker compose -f docker-compose.run.yml ps` 确认容器在运行。

**数据存在哪？**  
Docker 卷 `badminton-data`，重装不会丢（除非删卷）。

**如何备份？**

```bash
docker compose -f /root/badminton-court-manager/docker-compose.run.yml exec app cat /data/data.json > backup.json
```

---

## 与 Render 方案对比

| | 国内云服务器 | Render + GitHub |
|--|-------------|-----------------|
| 需要 GitHub | ❌ 不需要 | ✅ 需要 |
| 国内访问速度 | ✅ 快 | 一般 |
| 固定链接 | ✅ IP 或域名 | ✅ onrender.com |
| 费用 | 约 50 元/月 | 免费（有限制） |
