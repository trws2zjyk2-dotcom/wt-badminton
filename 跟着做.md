# 跟着做：10 分钟获得永久链接

按顺序完成下面 3 步。全程用浏览器，**以后不用再装任何软件**。

---

## 第 1 步：上传代码到 GitHub

### 1.1 注册 GitHub

打开 https://github.com/signup 注册（已有账号可跳过）

### 1.2 创建仓库

1. 打开 https://github.com/new
2. Repository name 填：`badminton-court-manager`
3. 选择 **Private**（私有，别人看不到你的代码）
4. **不要**勾选 "Add a README"
5. 点击 **Create repository**

### 1.3 推送代码

创建完成后，GitHub 会显示命令。在 Mac **终端**里运行（把 `你的用户名` 换成你的 GitHub 用户名）：

```bash
cd /Users/rae/Documents/badminton-court-manager
git branch -M main
git remote add origin https://github.com/你的用户名/badminton-court-manager.git
git push -u origin main
```

> 第一次 push 会弹出登录窗口，按提示用浏览器授权即可。

✅ 完成后刷新 GitHub 仓库页面，应能看到所有文件。

---

## 第 2 步：部署到 Render（获得链接）

### 2.1 注册 Render

1. 打开 https://render.com
2. 点击 **Get Started**，用 **GitHub 账号**登录并授权

### 2.2 一键部署

1. 点击 **New +** → **Blueprint**
2. 连接 GitHub，选择 `badminton-court-manager` 仓库
3. Render 会自动识别 `render.yaml`，点击 **Apply**

### 2.3 设置密码（重要）

在环境变量列表中找到 `ADMIN_PASSWORD`，点击编辑，填入你的登录密码，例如：

```
WtBadminton2024!
```

点击 **Apply** 开始部署，等待 2–3 分钟。

### 2.4 复制你的链接

部署成功后，页面顶部会显示：

```
https://badminton-court-manager-xxxx.onrender.com
```

**这就是你的永久链接！** 先收藏起来。

此时已可登录使用（账号 `admin`，密码为你刚设置的）。

---

## 第 3 步：配置云数据库（防止数据丢失）

> 跳过此步也能用，但 Render 免费版重启后数据会清空。**强烈建议完成。**

### 3.1 注册 Supabase

1. 打开 https://supabase.com
2. **Start your project** → 用 GitHub 登录
3. **New project** → 名称随意 → 设数据库密码 → Region 选 **Singapore**
4. 等待约 1 分钟创建完成

### 3.2 建表

1. 左侧 **SQL Editor** → **New query**
2. 粘贴以下内容，点击 **Run**：

```sql
create table if not exists app_data (
  id int primary key,
  data jsonb not null default '{"members":[],"bookings":[],"holidays":[]}'::jsonb,
  updated_at timestamptz default now()
);

insert into app_data (id, data)
values (1, '{"members":[],"bookings":[],"holidays":[]}'::jsonb)
on conflict (id) do nothing;
```

### 3.3 复制密钥

1. 左侧 **Project Settings**（齿轮）→ **API**
2. 复制 **Project URL**（如 `https://abcdefgh.supabase.co`）
3. 复制 **service_role** 密钥（点 Reveal 显示，**不要泄露给他人**）

### 3.4 填入 Render

1. 回到 Render → 你的服务 → 左侧 **Environment**
2. 添加两个变量：

| Key | Value |
|-----|-------|
| `SUPABASE_URL` | 刚才复制的 Project URL |
| `SUPABASE_SERVICE_KEY` | 刚才复制的 service_role 密钥 |

3. 点击 **Save Changes**，等待自动重新部署

---

## 完成 🎉

打开你的 Render 链接，用 `admin` + 你设置的密码登录。

- 任何电脑、手机浏览器都能打开
- 数据保存在云端，多设备同步
- 电脑可以关机，不影响使用

---

## 遇到问题？

| 问题 | 解决办法 |
|------|----------|
| 打开链接很慢 | 免费版休眠，等 30 秒即可 |
| 登录失败 | 检查 Render 里 `ADMIN_PASSWORD` 是否已设置 |
| git push 失败 | 确认 GitHub 用户名正确，重新浏览器授权 |
| 数据又没了 | 检查是否完成了第 3 步 Supabase 配置 |

需要帮助时，告诉我卡在哪一步。
